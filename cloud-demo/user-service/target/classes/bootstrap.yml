spring:
  application:
    name: userservice # 服务名称
  profiles:
    active: public #开发环境，这里是dev
  cloud:
    nacos:
      server-addr: nacos:8848 # Nacos地址
      config:
        file-extension: yaml # 文件后缀名